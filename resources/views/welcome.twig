<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to {{ hotel_name }}</title>
    <!-- Bootstrap CSS for styling and components -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
            padding-top: 5rem; /* Offset for fixed navbar */
        }
        header {
            background-color: #0056b3;
            color: #ffffff;
            padding: 1rem 0;
        }
        footer {
            background-color: #222;
            color: #fff;
            padding: 1rem;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
        .hero-image {
            width: 100%;
            height: 60vh;
            background: url('https://source.unsplash.com/1600x900/?hotel,Montreal') center no-repeat;
            background-size: cover;
        }
    </style>
</head>
<body>
    <!-- Fixed navbar using Bootstrap's navbar component -->
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">{{ hotel_name }}</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <ul class="navbar-nav me-auto mb-2 mb-md-0">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ route('rooms.index') }}">View Rooms</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ route('reserve.create') }}">Make a Reservation</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <header class="hero-image"></header>

    <main class="container">
        <h1 class="mt-4">Welcome to {{ hotel_name }}</h1>
        <p class="lead">Nestled in the heart of Montreal, {{ hotel_name }} offers an unparalleled experience for travelers seeking a memorable stay. Enjoy luxurious accommodations with stunning views, gourmet dining options, and world-class amenities.</p>
        <p>From our prime location, explore the vibrant culture and rich history of Montreal. Our dedicated staff ensures your stay is perfect, down to the last detail.</p>
        <div class="text-center">
            <img src="https://source.unsplash.com/800x600/?hotel-room" class="img-fluid my-3" alt="Elegant hotel room">
            <p>Discover our world-class services and enjoy your stay at the best prices!</p>
        </div>
    </main>

    <footer>
        <p>&copy; {{ "now"|date("Y") }} {{ hotel_name }}. All rights reserved.</p>
    </footer>

    <!-- Bootstrap Bundle with Popper.js for interactive components -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
